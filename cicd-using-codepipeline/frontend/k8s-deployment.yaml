apiVersion: apps/v1
kind: Deployment
metadata:
  name: my-student-management-client
  namespace: default
spec:
  selector:
    matchLabels:
      app: my-student-management-client
  replicas: 2
  template:
    metadata:
      labels:
        app: my-student-management-client
    spec:
      containers:
        - name: my-student-management-client
          image: IMAGE_URI
          imagePullPolicy: Always
          ports:
            - containerPort: 3000
          env:
            - name: REACT_APP_API_URL
              value: "http://my-student-management-api-service:8080"
---
apiVersion: v1
kind: Service
metadata:
  name: my-student-management-client-service
spec:
  selector:
    app: my-student-management-client
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: ClusterIP
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: my-student-management-ingress
  annotations:
    kubernetes.io/ingress.class: alb
    alb.ingress.kubernetes.io/scheme: internet-facing
    alb.ingress.kubernetes.io/target-type: ip
spec:
  rules:
    - http:
        paths:
          - path: /api
            pathType: Prefix
            backend:
              service:
                name: my-student-management-api-service
                port:
                  number: 8080
          - path: /
            pathType: Prefix
            backend:
              service:
                name: my-student-management-client-service
                port:
                  number: 80